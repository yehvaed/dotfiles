#!/usr/bin/env tmux source-file
#: ln -s @current ${HOME}/.tmux.conf

#==> plugin list
set -g @plugin 'b0o/tmux-autoreload'

# ==> custom config keybindings
bind-key -T root "M-h" select-pane -L
bind-key -T root "M-j" select-pane -D
bind-key -T root "M-k" select-pane -U
bind-key -T root "M-l" select-pane -R

# don't do anything when a 'bell' rings
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# clock mode
setw -g clock-mode-colour white

# copy mode
setw -g mode-style 'fg=white bg=white bold'

# pane borders
set -g pane-border-style 'fg=white'
set -g pane-active-border-style 'fg=colour3'

# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-style 'fg=white'
set -g status-left ''
set -g status-right '#[fg=orange bold]  #S '
set -g status-right-length 50
set -g status-left-length 10
setw -g window-status-current-style 'fg=white bg=red bold'
setw -g window-status-current-format ' #I #W #F '
setw -g window-status-style 'fg=black bg=yellow'
setw -g window-status-format ' #I #W #F '
setw -g window-status-bell-style 'fg=colour2 bg=white bold'

# messages
set -g message-style 'fg=colour2 bg=colour0 bold'
 

bind ":" command-prompt -p "tmux "

if "test ! -d ~/.local/share/tmux/tplug" \
   "run 'git clone https://github.com/yehvaed/tplug ~/.local/share/tmux/tplug'"

run "~/.local/share/tmux/tplug/init.sh"

# ==> configs
setw -g mouse on
setw -g mode-keys vi
set -g status on
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on
set -g status-left-style 'fg=white'
set -g message-style 'fg=white bg=black bold'
